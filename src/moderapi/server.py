"""FastAPI drop-in replacement server for Perspective API (Phase 2).

Endpoint: POST /v1alpha1/comments:analyze
Accepts the exact Perspective API request format.
Returns calibrated Detoxify scores in Perspective API response format.
"""

from __future__ import annotations


def create_app():
    """Create the FastAPI application. Phase 2 — not yet implemented."""
    raise NotImplementedError("Server is Phase 2 — build only if hard gate passes")

services:
  moderapi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./calibration.json:/app/calibration.json:ro
    environment:
      - MODERAPI_LOG_LEVEL=INFO
    restart: unless-stopped
